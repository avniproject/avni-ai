openapi: 3.0.0
info:
  title: Avni AI Tools
  version: 1.0.0
  description: Avni app configurator tools for entity validation, spec generation, and bundle management
servers:
  - url: https://staging-ai.avniproject.org
paths:
  /store-auth-token:
    post:
      operationId: store_auth_token
      summary: Store an Avni auth token by conversation_id for automatic resolution
      description: Call this once at the start of a conversation. All subsequent tool calls that need auth can just pass conversation_id instead of the avni-auth-token header.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation_id:
                  type: string
                  description: Dify conversation ID (sys.conversation_id)
                auth_token:
                  type: string
                  description: Avni authentication token
              required:
                - conversation_id
                - auth_token
      responses:
        '200':
          description: Token stored
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  conversation_id:
                    type: string

  /store-entities:
    post:
      operationId: store_entities
      summary: Cache entities server-side by conversation_id (TTL 6h)
      description: Call this once after entity extraction. Agent tools can then pass only conversation_id instead of the full entities payload.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation_id:
                  type: string
                  description: Dify conversation ID (sys.conversation_id)
                entities:
                  type: object
                  description: Full extracted entities dict
              required:
                - conversation_id
                - entities
      responses:
        '200':
          description: Entities cached
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean

  /validate-entities:
    post:
      operationId: validate_entities
      summary: Validate extracted entities for structural issues
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation_id:
                  type: string
                  description: Dify conversation ID — server looks up entities from store
              required:
                - conversation_id
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  issues:
                    type: array
                    items:
                      type: object
                  error_count:
                    type: integer
                  warning_count:
                    type: integer
                  issues_summary:
                    type: string
                  has_errors:
                    type: boolean
                  has_warnings:
                    type: boolean
                  entities:
                    type: object

  /apply-entity-corrections:
    post:
      operationId: apply_entity_corrections
      summary: Apply corrections to extracted entities
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation_id:
                  type: string
                  description: Dify conversation ID — server looks up and updates stored entities
                corrections:
                  type: array
                  description: Array of correction objects specifying changes to apply
                  items:
                    type: object
              required:
                - conversation_id
                - corrections
      responses:
        '200':
          description: Corrected entities
          content:
            application/json:
              schema:
                type: object
                properties:
                  result:
                    type: object

  /generate-spec:
    post:
      operationId: generate_spec
      summary: Generate a human-readable YAML spec from entities
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                conversation_id:
                  type: string
                  description: Dify conversation ID — server looks up entities from store
                org_name:
                  type: string
                  description: Organisation name
              required:
                - conversation_id
      responses:
        '200':
          description: Generated YAML spec
          content:
            application/json:
              schema:
                type: object
                properties:
                  spec_yaml:
                    type: string

  /validate-spec:
    post:
      operationId: validate_spec
      summary: Validate a YAML spec for structural and cross-reference errors
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                spec_yaml:
                  type: string
                  description: YAML spec string to validate
              required:
                - spec_yaml
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  valid:
                    type: boolean
                  errors:
                    type: array
                    items:
                      type: string
                  warnings:
                    type: array
                    items:
                      type: string
                  suggestions:
                    type: array
                    items:
                      type: string

  /spec-to-entities:
    post:
      operationId: spec_to_entities
      summary: Convert a YAML spec to a full entities dict for bundle generation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                spec_yaml:
                  type: string
                  description: Approved YAML spec string
              required:
                - spec_yaml
      responses:
        '200':
          description: Full entities dict
          content:
            application/json:
              schema:
                type: object
                properties:
                  entities:
                    type: object

  /bundle-to-spec:
    post:
      operationId: bundle_to_spec
      summary: Convert an existing bundle to a human-readable YAML spec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bundle:
                  type: object
                  description: Bundle object to convert
                org_name:
                  type: string
                  description: Organisation name
      responses:
        '200':
          description: YAML spec derived from bundle
          content:
            application/json:
              schema:
                type: object
                properties:
                  spec_yaml:
                    type: string

  /generate-bundle:
    post:
      operationId: generate_bundle
      summary: Generate a complete Avni bundle ZIP from entities
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                entities:
                  type: object
                  description: Full entities dict
                org_name:
                  type: string
                  description: Organisation name
              required:
                - entities
      responses:
        '200':
          description: Generated bundle as base64 ZIP
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundle_zip_b64:
                    type: string

  /validate-bundle:
    post:
      operationId: validate_bundle
      summary: Validate a generated bundle for structural correctness
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bundle:
                  type: object
                  description: Bundle JSON dict to validate (not base64, not a ZIP)
                bundle_zip_b64:
                  type: string
                  description: Base64 encoded bundle ZIP — alternative to bundle dict; server decodes internally
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                type: object
                properties:
                  valid:
                    type: boolean
                  errors:
                    type: array
                    items:
                      type: string
                  warnings:
                    type: array
                    items:
                      type: string

  /patch-bundle:
    post:
      operationId: patch_bundle
      summary: Patch an existing bundle ZIP with a new spec
      description: Decodes the existing bundle, generates a fresh bundle from the spec, and applies patches (voiding stale forms/mappings).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                existing_bundle_b64:
                  type: string
                  description: Base64 encoded existing bundle ZIP
                spec_yaml:
                  type: string
                  description: Approved YAML spec string to generate the patched bundle from
              required:
                - existing_bundle_b64
                - spec_yaml
      responses:
        '200':
          description: Patched bundle
          content:
            application/json:
              schema:
                type: object
                properties:
                  patched_bundle_b64:
                    type: string
                  files_patched:
                    type: integer
        '400':
          description: Bad request — malformed body or invalid spec
        '401':
          description: Unauthorized — missing or expired auth token
        '403':
          description: Forbidden — insufficient permissions
        '404':
          description: Not found
        '500':
          description: Internal server error

  /upload-bundle:
    post:
      operationId: upload_bundle
      summary: Upload a bundle ZIP to Avni server for import
      description: Auth is resolved automatically if conversation_id is provided and a token was stored via /store-auth-token. Otherwise pass avni-auth-token header.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                bundle_zip_b64:
                  type: string
                  description: Base64 encoded bundle ZIP to upload
                conversation_id:
                  type: string
                  description: Dify conversation ID — used to look up stored auth token
              required:
                - bundle_zip_b64
      responses:
        '200':
          description: Upload task created
          content:
            application/json:
              schema:
                type: object
                properties:
                  task_id:
                    type: string
                  status:
                    type: string
                    enum: [pending]
        '400':
          description: Bad request — malformed bundle
        '401':
          description: Unauthorized — missing or expired auth token
        '500':
          description: Internal server error

  /upload-status/{task_id}:
    get:
      operationId: upload_status
      summary: Check the status of a bundle upload task
      parameters:
        - name: task_id
          in: path
          required: true
          schema:
            type: string
          description: The upload task ID returned by upload-bundle
      responses:
        '200':
          description: Upload status
          content:
            application/json:
              schema:
                type: object
                properties:
                  task_id:
                    type: string
                  status:
                    type: string
                    enum: [pending, processing, completed, failed]
                  progress:
                    type: string
                  result:
                    type: object
                  error:
                    type: string

  /api/existing-config:
    get:
      operationId: get_existing_config
      summary: Download the current organisation configuration from Avni
      parameters:
        - name: includeLocations
          in: query
          required: false
          schema:
            type: boolean
          description: Whether to include location data
      responses:
        '200':
          description: Current organisation bundle
          content:
            application/json:
              schema:
                type: object

  /download-bundle-b64:
    get:
      operationId: download_bundle_b64
      summary: Download existing org bundle as base64 encoded ZIP
      description: Auth is resolved automatically if conversation_id is provided and a token was stored via /store-auth-token.
      parameters:
        - name: includeLocations
          in: query
          required: false
          schema:
            type: boolean
        - name: conversation_id
          in: query
          required: false
          schema:
            type: string
          description: Dify conversation ID — used to look up stored auth token
      responses:
        '200':
          description: Bundle as base64
          content:
            application/json:
              schema:
                type: object
                properties:
                  bundle_zip_b64:
                    type: string
        '400':
          description: Bad request
        '401':
          description: Unauthorized — missing or expired auth token
        '403':
          description: Forbidden — insufficient permissions
        '404':
          description: Not found
        '500':
          description: Internal server error

  /execute-python:
    post:
      operationId: execute_python
      summary: Execute Python code in a sandboxed environment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  description: Python code to execute
                context:
                  type: object
                  description: Variables available in the execution context
              required:
                - code
      responses:
        '200':
          description: Execution result
          content:
            application/json:
              schema:
                type: object
                properties:
                  output:
                    type: string
                  result:
                    type: object
                  error:
                    type: string
