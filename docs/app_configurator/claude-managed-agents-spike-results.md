# Spike results — Claude Managed Agents vs Agent SDK

_Skeleton; populated after the live run. The Comparison Matrix block is
auto-overwritten by `scripts/spike/compare_runs.py`._

## Goal
Decide whether to migrate the App Configurator orchestration from Dify v3 to
Anthropic Managed Agents. The SDK runner is the local-first fallback we keep
running for parity, not a long-term plan.

## Setup
- **Skills uploaded:** 6 — `srs-bundle-generator`, `project-scoping`,
  `avni-implementation-engineer`, `avni-support-engineer`, `support-patterns`,
  `architecture-patterns` (manifest at
  `src/orchestrators/claude_agent/skills.json`).
- **Agents:** Spec (Opus 4.5), Review (Sonnet 4.5), Error (Sonnet 4.5).
- **MCP:** avni-ai FastMCP, exposed via `cloudflared` Quick Tunnel for the
  Managed runner; localhost for the SDK runner.
- **SRS files used:** TBD (Yenepoya / Kshamata).
- **avni-server target:** TBD staging.
- **Date of run:** TBD.

## Comparison matrix
<!-- The block below is regenerated by compare_runs.py. -->

| Runner | Runs | Done | Latency p50 (s) | Latency p95 (s) | Mean tokens (in / out) | Mean cost (USD) | Tool calls (fail) | Clarifications | Phases | Review (A/R/–) |
|---|---|---|---|---|---|---|---|---|---|---|
| managed | – | – | – | – | – | – | – | – | – | – |
| sdk | – | – | – | – | – | – | – | – | – | – |

## What we measured

For each runner we capture, per RunRecord:
- Wall-clock latency for the full Spec → Review → (Error) loop.
- Input + output tokens (and derived cost at current Anthropic pricing).
- Tool calls + tool_call_failures.
- Clarifications asked (AskUserQuestion fires).
- Phases actually driven (`phases_run`).
- Review outcome (`approve` / `revise` / unset).

## Qualitative observations

_Filled in after the run — at minimum, one paragraph each on:_

1. Did the Spec Agent ask the right clarifying questions, or guess?
2. Did the Review Agent catch validation issues the Spec Agent missed?
3. Was the Error Agent useful on the injected failure (M5)?
4. Did the SDK runner diverge in any meaningful way from Managed?

## Recommendation
TBD — fill after the run. Three plausible outcomes:

- **Migrate now.** Managed Agents matches or beats Dify v3 on quality + cost,
  with materially better self-healing.
- **Migrate next quarter.** Promising but the beta has rough edges (specific
  ones noted above) we want fixed first.
- **Stay on Dify.** Either the cost or the operational maturity gap is too big.

## Open questions / follow-ups

_Filled in after the run._
